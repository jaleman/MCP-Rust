# Prior Rust Knowledge Established

Has completed a couple of Rust tutorials — knows basic syntax, ownership and borrowing concepts, and basic structs/enums. Has not yet worked with async/await, Tokio, traits at depth, or Cargo workspaces. Rust is the primary learning vehicle for this project, not a prerequisite.

**Implications**: Lessons must not assume async Rust knowledge. Introduce Tokio and async/await explicitly before using them in the MCP server code. Frame Rust concepts in terms of the concrete problem (e.g., "ownership matters here because...") rather than abstractly.
